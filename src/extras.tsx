//validating name

// const handleNameChange = (event: React.ChangeEvent<HTMLInputElement>) => {
//   name.current = event.target.value;
//   var validName = /^[a-zA-Z]{2,10}$/;
//   var result = validName.test(event.target.value);
//   setCheckNameValidity(result);
//   console.log(result);
//   if (result == false && name.current.length > 0)
//     setNameError("Not a Valid Name");
//   else {
//     setNameError("Enter Your Name");
//   }
// };
